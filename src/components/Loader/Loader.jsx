import { RiseLoader } from 'react-spinners'

const Loader = () => {
    return (
        <div className={"loader"}>
            <RiseLoader color="#007dff"/>
        </div>
    )
}

export default Loader